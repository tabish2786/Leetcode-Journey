class Solution(object):
    def maxProfit(self, prices):
        """
        :type prices: List[int]
        :rtype: int
        """
        buy = prices[0]
        profit = 0
        for i in prices:
            buy = min(buy, i)
            profit = max(profit, i - buy)
        return profit